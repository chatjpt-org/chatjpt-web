:80 {
  encode zstd gzip

  route {
    handle_path /api/* {
      reverse_proxy chatjpt-api:8080 {
        flush_interval -1
      }
    }

    handle {
      try_files {path} /index.html
      file_server
    }
  }
}
